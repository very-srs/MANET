# Refuse to start a mesh supplicant on the radio hostapd is currently serving.
# See /usr/local/bin/manet-ap-guard.sh for why this is a property of the unit
# rather than a check in each of the callers.
[Service]
ExecCondition=/usr/local/bin/manet-ap-guard.sh %i
