*.img
*.deb
*.tar.gz
*.tar.gz.sha256
*.tar.gz.sha256.tmp

# Compressed OS images and their checksums (provisioning/)
*.img.xz
*.img.sha256

# Build output and backups kept beside the real artifacts
__pycache__/
*.prev-*
install_packages-orig/

# Kernel build infrastructure — deliberately not in this repo
kernel-work/

# Build tooling. The published repo is the user's manual, not a dev guide.
MANET/packaging/

# Working folder a standalone flasher copy makes for itself
manet-flasher/
