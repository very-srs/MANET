# IMPORTANT:
# Replace the placeholder values below with the actual network
# interface names from your host machine.
#
# You can find your interface names by running the `ip addr` or `ifconfig`
# command on your host's terminal.

# The interface that provides the internet connection (e.g., your WiFi card)
WIFI_IFACE=wlp3s0

# The interface that will serve DHCP to clients (e.g., your Ethernet port)
ETH_IFACE=enp0s31f6
